[user]
    name = knock
    email = dff9th@gmail.com
[alias]
    a = add
    A = add -A
    b = branch
    ba = branch -a
    c = config
    ch = checkout
    cl = clone
    cm = commit
    d = diff
    dn = diff --name-only
    f = fetch
    fa = fetch --all
    l = log
    lg = log --graph
    ll = log --oneline
    m = merge
    s = status
[core]
    editor = $GIT_EDITOR -c \"set fenc=utf-8\"

